# Helm packaging artifacts
*.tgz

# Fetched chart dependencies
**/charts/
**/Chart.lock

# Editor / OS
.DS_Store
*.swp

# Assistant working directory, and the git worktrees other sessions keep under
# it. Those are repositories of their own: staged wholesale they land here as
# gitlinks pointing at commits nobody else has.
.claude/
