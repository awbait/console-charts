{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/namespace/values.schema.json",
  "title": "namespace Helm chart values",
  "description": "Editor-oriented schema for the namespace chart (cpaas Namespace, ResourceQuota, kube-ovn Subnet).",
  "type": "object",
  "additionalProperties": false,
  "propertyOrder": [
    "identity",
    "namespace",
    "resourceQuotas",
    "subnet",
    "generic",
    "serviceMesh",
    "global"
  ],
  "required": [
    "identity",
    "namespace",
    "resourceQuotas"
  ],
  "properties": {
    "identity": {
      "title": "Окружение и проект",
      "description": "Куда встанет заказ и к какому проекту он относится. Задаётся один раз на весь заказ.",
      "$ref": "#/definitions/identity"
    },
    "namespace": {
      "title": "Неймспейс",
      "description": "Как неймспейс будет называться и где он появится.",
      "$ref": "#/definitions/namespaceSpec"
    },
    "resourceQuotas": {
      "title": "Квоты ресурсов",
      "description": "Сколько ресурсов смогут занять сервисы этого неймспейса вместе.",
      "$ref": "#/definitions/resourceQuotas"
    },
    "subnet": {
      "title": "Подсети неймспейса",
      "description": "Выделенная подсеть для подов. Нужна, только если диапазон адресов уже забронирован.",
      "$ref": "#/definitions/subnet"
    },
    "generic": {
      "title": "Generic settings",
      "description": "Common labels/annotations applied to all chart resources (Namespace, ResourceQuota, Subnet).",
      "ui:widget": "hidden",
      "$ref": "#/definitions/generic"
    },
    "serviceMesh": {
      "title": "Service Mesh",
      "description": "Istio ambient integration, switched on at the environment level. When enabled the Namespace gets the istio-discovery and istio.io/dataplane-mode labels and the networking.k8s.io/enable-netpol annotation.",
      "ui:widget": "hidden",
      "$ref": "#/definitions/serviceMesh"
    },
    "global": {
      "title": "global",
      "description": "Chart-wide switch. Set global.enable to false to render no resources at all.",
      "ui:widget": "hidden",
      "$ref": "#/definitions/global"
    }
  },
  "definitions": {
    "projectToken": {
      "type": "string",
      "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
      "minLength": 2,
      "maxLength": 9
    },
    "dnsName": {
      "type": "string",
      "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
      "maxLength": 63
    },
    "quantity": {
      "type": "string"
    },
    "stringMap": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "identity": {
      "type": "object",
      "additionalProperties": false,
      "propertyOrder": [
        "instance",
        "cluster",
        "project"
      ],
      "properties": {
        "instance": {
          "title": "Инстанс",
          "description": "Выбирайте вместе с кластером, значения идут парой: in - Infra-dev-ecpk, id - dev-ecpk, ed - techsec-dev, dev-common и test-common, pr - global, observability, techsec, int-common, ML и ext-common.",
          "type": "string",
          "enum": [
            "in",
            "id",
            "ed",
            "pr"
          ]
        },
        "cluster": {
          "title": "Кластер",
          "description": "Кластер, куда встанет сам заказ: inf - Infra-dev-ecpk, dev - dev-ecpk и dev-common, tco - techsec-dev, tst - test-common, gl - global, obs - observability, tcc - techsec, cmi - int-common, ml - ML, cme - ext-common.",
          "type": "string",
          "enum": [
            "inf",
            "dev",
            "tco",
            "tst",
            "gl",
            "obs",
            "tcc",
            "cmi",
            "ml",
            "cme"
          ]
        },
        "project": {
          "title": "Проект",
          "description": "Короткий код проекта. Попадает в метки, по которым ресурсы находят в кластере.",
          "$ref": "#/definitions/projectToken"
        }
      },
      "required": [
        "instance",
        "cluster",
        "project"
      ]
    },
    "namespaceSpec": {
      "type": "object",
      "additionalProperties": false,
      "propertyOrder": [
        "namespaceName",
        "projectName",
        "clusterName",
        "displayName",
        "role",
        "labels",
        "annotations",
        "creator"
      ],
      "properties": {
        "namespaceName": {
          "title": "Имя неймспейса",
          "description": "Имя, под которым неймспейс появится в кластере. Принято складывать его из продукта, среды и назначения: nbox-dev-ns-app.",
          "$ref": "#/definitions/dnsName"
        },
        "projectName": {
          "title": "Проект-владелец",
          "description": "Проект платформы, внутри которого создаётся неймспейс.",
          "type": "string"
        },
        "clusterName": {
          "title": "Кластер размещения",
          "description": "Кластер платформы, в котором появится неймспейс.",
          "type": "string"
        },
        "displayName": {
          "title": "Отображаемое имя",
          "description": "То же имя обычными словами. Видно в консоли платформы.",
          "type": "string"
        },
        "role": {
          "title": "Роль неймспейса",
          "description": "ingress - здесь живут точки входа трафика, egress - точки выхода, other - обычные сервисы.",
          "type": "string",
          "enum": [
            "ingress",
            "egress",
            "other"
          ]
        },
        "labels": {
          "title": "Дополнительные labels",
          "description": "Ставятся только на сам неймспейс, не на квоты и подсети.",
          "$ref": "#/definitions/stringMap"
        },
        "annotations": {
          "title": "Дополнительные annotations",
          "description": "Ставятся только на сам неймспейс, не на квоты и подсети.",
          "$ref": "#/definitions/stringMap"
        },
        "creator": {
          "title": "creator",
          "description": "Namespace creator (annotation cpaas.io/creator). The portal stamps \"console\" on an order.",
          "type": "string",
          "default": "lk",
          "ui:widget": "hidden"
        }
      },
      "required": [
        "namespaceName",
        "projectName"
      ]
    },
    "resourceQuotas": {
      "type": "object",
      "additionalProperties": false,
      "propertyOrder": [
        "limits",
        "requests",
        "storage",
        "podsAmount",
        "name"
      ],
      "properties": {
        "limits": {
          "title": "Лимиты",
          "description": "Верхняя граница на все сервисы неймспейса вместе.",
          "type": "object",
          "additionalProperties": false,
          "propertyOrder": [
            "cpu",
            "memory"
          ],
          "properties": {
            "cpu": {
              "title": "Лимит CPU",
              "description": "Сколько ядер суммарно. Например 4 или 500m.",
              "$ref": "#/definitions/quantity"
            },
            "memory": {
              "title": "Лимит памяти",
              "description": "Например 8Gi или 512Mi.",
              "$ref": "#/definitions/quantity"
            }
          },
          "required": [
            "cpu",
            "memory"
          ]
        },
        "requests": {
          "title": "Запросы",
          "description": "Сколько выделяется сервисам гарантированно.",
          "type": "object",
          "additionalProperties": false,
          "propertyOrder": [
            "cpu",
            "memory"
          ],
          "properties": {
            "cpu": {
              "title": "Запрос CPU",
              "description": "Пустое поле - берётся лимит CPU.",
              "$ref": "#/definitions/quantity"
            },
            "memory": {
              "title": "Запрос памяти",
              "description": "Пустое поле - берётся лимит памяти.",
              "$ref": "#/definitions/quantity"
            }
          }
        },
        "storage": {
          "title": "Квоты на хранилище",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "storages": {
              "title": "Объём по классам хранилища",
              "description": "Ключ - класс хранилища, значение - объём. Например nfs: 100Gi.",
              "$ref": "#/definitions/stringMap"
            }
          }
        },
        "podsAmount": {
          "title": "podsAmount",
          "description": "Maximum number of pods in the Namespace. Bounded by the subnet mask.",
          "type": "integer",
          "default": 1000,
          "ui:widget": "hidden"
        },
        "name": {
          "title": "name",
          "description": "Name of the ResourceQuota resource. Defaults to \"default\".",
          "type": "string",
          "default": "default",
          "ui:widget": "hidden"
        }
      },
      "required": [
        "limits"
      ]
    },
    "subnet": {
      "type": "object",
      "additionalProperties": false,
      "propertyOrder": [
        "subnets",
        "enable"
      ],
      "properties": {
        "enable": {
          "title": "enable",
          "description": "Service switch for the Subnet resource. Turn it on only when the CIDR block is already reserved in Netbox.",
          "type": "boolean",
          "default": false,
          "ui:widget": "hidden"
        },
        "subnets": {
          "title": "Подсети",
          "description": "Обычно нужна одна подсеть, но их может быть несколько.",
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/definitions/subnetEntry"
          }
        }
      }
    },
    "subnetEntry": {
      "type": "object",
      "additionalProperties": false,
      "propertyOrder": [
        "name",
        "cidrBlock",
        "allowSubnets",
        "excludeIps"
      ],
      "properties": {
        "name": {
          "title": "Имя подсети",
          "description": "Имя, под которым подсеть появится в кластере.",
          "$ref": "#/definitions/dnsName"
        },
        "cidrBlock": {
          "title": "Диапазон адресов",
          "description": "Забронированный диапазон вместе с маской. Например 10.24.8.0/22.",
          "type": "string"
        },
        "allowSubnets": {
          "title": "Разрешённые подсети",
          "description": "Диапазоны, с которыми этой подсети нужна сетевая связность.",
          "type": "array",
          "items": {
            "title": "Диапазон адресов",
            "type": "string"
          }
        },
        "excludeIps": {
          "title": "Зарезервированные адреса",
          "description": "Адреса, которые не будут выданы подам. Адрес шлюза исключается сам.",
          "type": "array",
          "items": {
            "title": "IP-адрес",
            "type": "string"
          }
        }
      },
      "required": [
        "name",
        "cidrBlock"
      ]
    },
    "generic": {
      "type": "object",
      "additionalProperties": false,
      "propertyOrder": [
        "labels",
        "annotations"
      ],
      "properties": {
        "labels": {
          "title": "Common labels",
          "description": "Common labels for all chart resources. Result: metadata.labels.",
          "$ref": "#/definitions/stringMap"
        },
        "annotations": {
          "title": "Common annotations",
          "description": "Common annotations for all chart resources. Result: metadata.annotations.",
          "$ref": "#/definitions/stringMap"
        }
      }
    },
    "serviceMesh": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "title": "enabled",
          "description": "Enable Service Mesh for the Namespace.",
          "type": "boolean",
          "default": false,
          "ui:widget": "hidden"
        }
      }
    },
    "global": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enable": {
          "title": "enable",
          "description": "Render the chart resources. Set to false when the chart is a subchart that must stay silent.",
          "type": "boolean",
          "default": true,
          "ui:widget": "hidden"
        }
      }
    }
  }
}
