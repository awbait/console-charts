# namespace

Чарт создаёт изолированное рабочее пространство в кластере: управляемый
платформой `Namespace`, квоты ресурсов и, при необходимости, выделенную сеть.

## Что создаёт чарт

| Что создаётся   | Когда                                                        |
|-----------------|--------------------------------------------------------------|
| `Namespace`     | Всегда, когда чарт включён                                   |
| `ResourceQuota` | Всегда: лимиты CPU, памяти, числа подов и хранилищ           |
| `Subnet`        | Когда включена выделенная подсеть                            |

Ресурсы создаются, только когда чарт включён. Это служебный флаг, его проставляет
консоль при заказе.

## Имена ресурсов

Имя каждого ресурса чарт собирает сам:

```
{identity.project}-{identity.cluster}-{ns|subnet}-{назначение}
```

Заказ называет только назначение - `global.namespacePurpose`. Остальное берётся
из `identity`, поэтому имя ресурса и его теги не могут разойтись. Примеры:
`nbox-dev-ns-app`, `nbox-dev-subnet-app`.

Тег проекта в форме заказа не спрашивают: портал подставляет туда команду, от
имени которой сделан заказ. Заполняют его, только когда чарт ставят напрямую.

Назначение лежит в `global`, потому что его читает и сабчарт `waypoint`: Helm
раздаёт `global` всем сабчартам, так что назначение вводится один раз, и оба
чарта приходят к одному неймспейсу.

| Поле                     | Что задаёт                                                       |
|--------------------------|--------------------------------------------------------------------|
| `global.namespacePurpose`| Назначение для всего релиза, включая сабчарты                      |
| `namespace.name`         | Переопределяет назначение только для самого неймспейса             |
| `subnet.subnets[].name`  | Назначение подсети, пусто - берётся назначение неймспейса          |

`namespace.name` сабчарты не видят. Вместе с включённым waypoint повторите то же
назначение в `waypoint.namespacePurpose`, иначе чарт остановит рендер и скажет
положить назначение в `global` или в поле сабчарта.

Назначение - 2..12 символов, строчные латинские буквы, цифры и дефис. Готовое имя
обрезается до 63 символов.

## Теги окружения и проекта

Секция `identity` задаётся один раз на релиз. `project` и `cluster` дают первые
две части имени каждого ресурса, и все три тега проставляются на ресурсы чарта
как labels, по которым ресурсы находятся в кластере.

В форме заказа секция скрыта: значения проставляет портал (блок `defaults` в
`view.schema.json`).

Когда чарт подключён сабчартом, родительский чарт может передать теги один раз
через `global.identity`: тогда они сильнее блока `identity` и уходят дальше, в
сабчарт `waypoint`. Так делает чарт `egress-gateway`, который поднимает этим
чартом два неймспейса.

| Поле                | Что это                                                                       |
|---------------------|--------------------------------------------------------------------------------|
| `identity.instance` | Тег инстанса окружения. Обязателен, значение из таблицы ниже                    |
| `identity.cluster`  | Тег кластера окружения. Обязателен, значение из таблицы ниже                    |
| `identity.project`  | Тег проекта. Обязателен, до 9 символов: строчные латинские буквы, цифры и дефис |

Эти же теги уходят в cpaas-поля неймспейса: `identity.project` - в label
`cpaas.io/project` и annotation `cpaas.io/project-inner-namespace`,
`identity.cluster` - в label `cpaas.io/cluster`. Отдельных `projectName` и
`clusterName` у чарта больше нет.

Пара тегов окружения:

| Окружение      | `identity.cluster` | `identity.instance` |
|----------------|--------------------|---------------------|
| Infra-dev-ecpk | `inf`              | `in`                |
| dev-ecpk       | `dev`              | `id`                |
| techsec-dev    | `tco`              | `ed`                |
| dev-common     | `dev`              | `ed`                |
| test-common    | `tst`              | `ed`                |
| global         | `gl`               | `pr`                |
| observability  | `obs`              | `pr`                |
| techsec        | `tcc`              | `pr`                |
| int-common     | `cmi`              | `pr`                |
| ML             | `ml`               | `pr`                |
| ext-common     | `cme`              | `pr`                |

Оба поля описаны в схеме значений перечислением. Берите значения строкой
таблицы: схема проверяет каждое поле по отдельности и не поймает пару из разных
окружений.

## Labels на ресурсах

| Label                          | Значение                                                  |
|--------------------------------|-----------------------------------------------------------|
| `app`                          | `namespace` - имя чарта, не зависит от имени релиза |
| `app.cpaas.io/name`            | Имя релиза                                                 |
| `app.kubernetes.io/managed-by` | `Helm`                                                     |
| `helm.sh/chart`                | Имя и версия чарта                                         |
| `ecpk/instance`                | `identity.instance`, если тег задан                        |
| `ecpk/cluster`                 | `identity.cluster`, если тег задан                         |
| `ecpk/project`                 | `identity.project`, если тег задан                         |

У `Namespace` вдобавок есть cpaas-labels `cpaas.io/cluster` и `cpaas.io/project`
из `identity` и `namespace-role` из `namespace.role`. Роль в форме заказа скрыта:
без явного значения ставится `other`.

`namespace.podSecurity` ставит на `Namespace` три метки Pod Security Standards
(`enforce`, `audit`, `warn`) с одним уровнем: `restricted`, `baseline` или
`privileged`. Пустое значение оставляет метки платформе.

Ресурсы несут аннотацию `argocd.argoproj.io/sync-wave`: `Namespace` идёт в волне
`-3`, `ResourceQuota` и `Subnet` в `-2`. Так Argo CD создаёт неймспейс и его
подсеть раньше всего, что чарт-родитель кладёт внутрь. Вне Argo CD аннотация ни
на что не влияет.

Свои labels и annotations добавляются через `generic.labels` и
`generic.annotations`. Они ложатся на все ресурсы релиза сразу: пометить
только `Namespace`, не задев квоты и подсети, нельзя.

## Что нужно указать

| Поле                      | Нужно указать | Что это                                                        |
|---------------------------|---------------|-----------------------------------------------------------------|
| `global.namespacePurpose` | да            | Назначение namespace, 2..12 символов: последняя часть имени      |
| `namespace.name`          | нет           | Переопределение назначения для самого namespace                 |
| `namespace.displayName`   | нет           | Отображаемое имя namespace                                      |
| `namespace.role`          | нет           | Роль namespace: `ingress`, `egress` или `other`, по умолчанию `other` |
| `namespace.podSecurity`   | нет           | Уровень Pod Security: `restricted`, `baseline` или `privileged`  |
| `resourceQuotas.limits`   | да            | Лимиты CPU и памяти, например `cpu: "4"` и `memory: "8Gi"`. Без них рендер останавливается |
| `resourceQuotas.requests` | нет           | Запросы CPU и памяти, по умолчанию берутся из лимитов           |
| `resourceQuotas.storage`  | нет           | Объём по классам хранилища, см. ниже                            |
| `subnet`                  | нет           | Выделенная подсеть, нужен зарезервированный CIDR                |

## Квоты на хранилище

`resourceQuotas.storage` - это объём на весь неймспейс по каждому классу
хранилища. Классы фиксированы платформой, свои в значениях не завести:

| Класс        | Что это                                                                     |
|--------------|------------------------------------------------------------------------------|
| `topolvm-mu` | Быстрый диск для частой записи. Берите его для очередей сообщений, журналов и аналитических баз |
| `topolvm-ri` | Экономичный диск для частого чтения. Берите его для кешей, поисковых индексов и данных, которые можно загрузить заново |

Пустое значение означает, что такой диск неймспейсу не выдаётся. Оба класса
держат данные на диске того сервера, где работает сервис, и копий на других
серверах не делают: восстановление после отказа - забота самого сервиса.

```yaml
resourceQuotas:
  storage:
    topolvm-mu: "100Gi"
    topolvm-ri: "200Gi"
```

## Подсеть

Подсеть стоит включать, только если CIDR заранее зарезервирован в Netbox. Сам
диапазон (`cidrBlock`) заполняет платформа: в форме заказа поле скрыто. Размер
подсети выбирает заказчик полем `mask`.

| Поле                        | Что это                                                                 |
|-----------------------------|--------------------------------------------------------------------------|
| `subnets[].name`            | Назначение подсети. Пусто - берётся назначение неймспейса                 |
| `subnets[].mask`            | Маска подсети, выбирается из списка. Задаёт, сколько подов поместится     |
| `subnets[].cidrBlock`       | Диапазон адресов вместе с маской, маска 16..30. Заполняет платформа       |
| `subnets[].reservedIps`     | Сколько адресов не отдавать подам, помимо адреса шлюза. От 0 до 16        |
| `subnets[].allowSubnets`    | Диапазоны, с которыми подсети нужна связность                            |
| `subnets[].private`         | Изолированная подсеть, по умолчанию `true`. `false` - подам разрешены любые адреса |
| `subnets[].natOutgoing`     | NAT исходящего трафика средствами kube-ovn, по умолчанию `false`         |

Маска 1:1 маппится в число подов: адреса сети, broadcast и шлюза подам не
достаются, поэтому мест на три меньше размера блока.

| Маска | Подов |
|-------|-------|
| `/29` | 5     |
| `/28` | 13    |
| `/27` | 29    |
| `/26` | 61    |
| `/25` | 125   |
| `/24` | 253   |

Платформа бронирует в Netbox диапазон с заказанной маской и вписывает его в
`cidrBlock`. Если маска пришедшего диапазона не совпадает с заказанной, рендер
останавливается.

Адреса заказ не перечисляет: он называет только их количество, потому что какие
адреса лежат в блоке, знает платформа, а не тот, кто заказывает. Чарт
отсчитывает их от начала блока: первый адрес - шлюз, за ним идут
зарезервированные. Для `10.24.8.0/24` и `reservedIps: 3` подам не достанутся
`10.24.8.1` (шлюз), `10.24.8.2`, `10.24.8.3` и `10.24.8.4`. Готовый список
виден в выводе установки.

Подсеть по умолчанию изолированная: её поды ходят только в разрешённые
подсети, остальное отбрасывается ещё на логическом свиче. Неймспейсу, чьи поды
сами ходят на внешние адреса (waypoint egress-шлюза, `VpcEgressGateway`,
отправители в режиме direct), подсеть заказывают с `private: false`. Остальные
параметры зафиксированы платформой и через значения не настраиваются.

## Service Mesh

Выбор есть только в кластере `dev`: там `serviceMesh.enabled` решает, будет ли
сетка. В остальных кластерах сетка - часть платформы, и флаг там не читается:
namespace получает метки Istio ambient и разрешение на сетевые политики в любом
случае.

`serviceMesh.waypoint` подключает сабчарт `waypoint` - прокси, через который
идёт обмен внутри namespace. Он нужен, когда правила доступа разбирают не только
адрес, но и сам запрос. Без включённой сетки рендер останавливается.

Namespace сабчарт берёт из `global.namespacePurpose` и собирает то же имя, что и
этот чарт, так что заполнять ничего не нужно. Свои `waypoint.namespaceOverride`
и `waypoint.namespacePurpose` перебивают `global`, поэтому чарт сверяет, куда
придёт сабчарт, с тем namespace, который создаёт сам, и останавливает рендер
при расхождении.

`serviceMesh.never` держит namespace вне сетки в любом кластере: правило «вне
dev сетка включена всегда» на него не действует, а на `Namespace` ставится
метка `istio.io/dataplane-mode: none`. Это для транзитных шлюзов, которые
ломаются от перехвата ambient: так чарт `egress-gateway` заказывает namespace
для `VpcEgressGateway`. Вместе с waypoint не сочетается.

## Когда рендер останавливается

- не заданы теги `identity` или их значения вне допустимых;
- `serviceMesh.waypoint` включён, а сетки нет или стоит `serviceMesh.never`;
- `serviceMesh.waypoint` включён, а сабчарт `waypoint` пришёл бы в другой
  namespace: назначение задано в `namespace.name` и не повторено в
  `waypoint.namespacePurpose`, либо значения `waypoint.*` указывают не туда;
- не задано назначение namespace, оно короче 2 или длиннее 12 символов либо
  записано не строчными латинскими буквами, цифрами и дефисом;
- `namespace.podSecurity` не `restricted`, не `baseline` и не `privileged`;
- не заданы лимиты CPU или памяти в `resourceQuotas.limits`;
- подсеть включена, но у неё не задан CIDR;
- CIDR подсети записан не в формате «адрес/маска», маска вне 16..30 либо адрес
  не совпадает с первым адресом блока;
- маска выданного платформой CIDR не совпадает с заказанной в `mask`;
- в блоке не хватает места на шлюз и запрошенное число зарезервированных адресов.

## Файлы чарта

| Файл                  | Для чего                                                       |
|-----------------------|-----------------------------------------------------------------|
| `values.yaml`         | Значения по умолчанию: минимум, при котором проходит рендер       |
| `values.minimal.yaml` | Короткий рабочий пример: namespace и квоты, без подсети          |
| `values.full.yaml`    | Полный справочник: все параметры с пояснениями                   |
| `values.schema.json`  | Схема значений: проверка при установке и форма заказа в портале  |
