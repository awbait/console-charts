{{- if and .Values.collector.enabled .Values.collector.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "console.collector.fullname" . }}
  labels:
    {{- include "console.labels" . | nindent 4 }}
    {{- include "console.collector.selectorLabels" . | nindent 4 }}
rules:
  # Список namespace (фильтр по метке - на стороне запроса).
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get", "list", "watch"]
  # Контроллеры, из podTemplate которых берётся каталог.
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "daemonsets"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "console.collector.fullname" . }}
  labels:
    {{- include "console.labels" . | nindent 4 }}
    {{- include "console.collector.selectorLabels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "console.collector.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "console.collector.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
