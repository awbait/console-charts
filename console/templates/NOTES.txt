console ({{ .Chart.Version }}, app {{ .Chart.AppVersion }}) развёрнут в namespace {{ .Release.Namespace | quote }}.

Компоненты:
  • portal (backend) - Service {{ include "console.portal.fullname" . }}:{{ .Values.portal.service.port }}
  • web (frontend)   - Service {{ include "console.web.fullname" . }}:{{ .Values.web.service.port }}

Вход (web) публикуется снаружи отдельно. Для локальной проверки пробросьте порт:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "console.web.fullname" . }} 8080:{{ .Values.web.service.port }}
  затем откройте http://localhost:8080

{{- if eq (toString (.Values.portal.config.AUTH_MODE | default "")) "dev" }}

ВНИМАНИЕ: AUTH_MODE=dev - аутентификация отключена (dev-пользователь). Не для прод.
{{- end }}
{{- if and .Values.web.devAuth.enabled (ne (toString (.Values.portal.config.AUTH_MODE | default "")) "dev") }}

ВНИМАНИЕ: web.devAuth.enabled=true при AUTH_MODE!=dev - nginx подменяет пользователя
заголовками X-Dev-*, перекрывая OIDC-сессию. Для прод выключите web.devAuth.
{{- end }}

Проверка:
  kubectl -n {{ .Release.Namespace }} get deploy,svc -l app.kubernetes.io/instance={{ .Release.Name }}
