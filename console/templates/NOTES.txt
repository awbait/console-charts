console ({{ .Chart.Version }}, app {{ .Chart.AppVersion }}) развёрнут в namespace {{ .Release.Namespace | quote }}.

portal (API + SPA) - Service {{ include "console.portal.fullname" . }}:{{ .Values.portal.service.port }}

Проверка:
  kubectl -n {{ .Release.Namespace }} get deploy,svc -l app.kubernetes.io/instance={{ .Release.Name }}
