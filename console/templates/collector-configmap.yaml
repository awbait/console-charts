{{- if .Values.collector.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "console.collector.fullname" . }}-config
  labels:
    {{- include "console.labels" . | nindent 4 }}
    {{- include "console.collector.selectorLabels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.collector.config }}
  {{- if ne (toString $v) "" }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
  {{- end }}
{{- end }}
