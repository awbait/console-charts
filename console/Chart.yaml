apiVersion: v2
name: console
description: "Helm chart for the IDP Console (self-service developer portal): Go backend (portal) + nginx-served SPA frontend (web)"
type: application
version: 0.1.0
appVersion: "0.1.0"
