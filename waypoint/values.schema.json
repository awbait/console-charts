{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/waypoint/values.schema.json",
  "title": "waypoint Helm chart values",
  "description": "Editor-oriented schema for the waypoint chart (Istio ambient namespace waypoint proxy, Gateway API).",
  "type": "object",
  "additionalProperties": false,
  "propertyOrder": [
    "identity",
    "generic",
    "waypoints"
  ],
  "required": [
    "identity"
  ],
  "properties": {
    "identity": {
      "title": "Identity",
      "description": "Environment and project tags for generating resource names by the convention {instance}-{cluster}-{kindShort}-{project}-{name} (kindShort: wp). The same tags are set as the ecpk/instance, ecpk/cluster and ecpk/project labels on every resource.",
      "$ref": "#/definitions/identity"
    },
    "generic": {
      "title": "Generic settings",
      "description": "Common labels/annotations applied to all chart resources.",
      "ui:widget": "hidden",
      "$ref": "#/definitions/generic"
    },
    "waypoints": {
      "title": "Waypoints",
      "description": "List of waypoint proxies. Each item creates a Gateway of class istio-waypoint (HBONE/15008).",
      "type": "array",
      "items": {
        "$ref": "#/definitions/waypoint"
      },
      "defaultSnippets": [
        {
          "label": "Waypoint",
          "body": [
            {
              "name": "mesh",
              "enabled": true,
              "for": "service"
            }
          ]
        }
      ]
    }
  },
  "definitions": {
    "k8sName": {
      "type": "string",
      "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
      "description": "Kubernetes DNS label: lower-case letters, digits and hyphens; no leading/trailing hyphen."
    },
    "shortToken": {
      "type": "string",
      "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
      "minLength": 2,
      "maxLength": 6,
      "description": "DNS token 2..6 characters: lower-case letters, digits and hyphens; no leading/trailing hyphen."
    },
    "projectToken": {
      "type": "string",
      "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
      "minLength": 2,
      "maxLength": 9,
      "description": "DNS token 2..9 characters: lower-case letters, digits and hyphens; no leading/trailing hyphen."
    },
    "tplStringMap": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "identity": {
      "type": "object",
      "additionalProperties": false,
      "propertyOrder": [
        "instance",
        "cluster",
        "project"
      ],
      "properties": {
        "instance": {
          "title": "instance",
          "description": "Instance tag of the target environment: in - Infra-dev-ecpk, id - dev-ecpk, ed - techsec-dev, dev-common and test-common, pr - global, observability, techsec, int-common, ML and ext-common.",
          "type": "string",
          "enum": [
            "in",
            "id",
            "ed",
            "pr"
          ]
        },
        "cluster": {
          "title": "cluster",
          "description": "Cluster tag of the target environment: inf - Infra-dev-ecpk, dev - dev-ecpk and dev-common, tco - techsec-dev, tst - test-common, gl - global, obs - observability, tcc - techsec, cmi - int-common, ml - ML, cme - ext-common.",
          "type": "string",
          "enum": [
            "inf",
            "dev",
            "tco",
            "tst",
            "gl",
            "obs",
            "tcc",
            "cmi",
            "ml",
            "cme"
          ]
        },
        "project": {
          "title": "project",
          "description": "Project tag. Up to 9 characters: lower-case letters, digits and hyphens.",
          "$ref": "#/definitions/projectToken"
        }
      },
      "required": [
        "instance",
        "cluster",
        "project"
      ]
    },
    "generic": {
      "type": "object",
      "additionalProperties": false,
      "propertyOrder": [
        "labels",
        "annotations"
      ],
      "properties": {
        "labels": {
          "title": "Common labels",
          "description": "Common labels for all chart resources. Result: metadata.labels.",
          "$ref": "#/definitions/tplStringMap"
        },
        "annotations": {
          "title": "Common annotations",
          "description": "Common annotations for all chart resources. Result: metadata.annotations.",
          "$ref": "#/definitions/tplStringMap"
        }
      }
    },
    "waypoint": {
      "type": "object",
      "additionalProperties": false,
      "propertyOrder": [
        "name",
        "enabled",
        "for"
      ],
      "properties": {
        "name": {
          "title": "name",
          "description": "Waypoint name, 2..6 characters. {name} in the Gateway name (kindShort wp).",
          "$ref": "#/definitions/shortToken"
        },
        "enabled": {
          "title": "enabled",
          "description": "true = create the Gateway; false = skip.",
          "type": "boolean",
          "default": true
        },
        "for": {
          "title": "for",
          "description": "istio.io/waypoint-for: the waypoint interception scope.",
          "type": "string",
          "enum": [
            "service",
            "workload",
            "all"
          ],
          "default": "service"
        }
      },
      "required": [
        "name"
      ]
    }
  }
}
