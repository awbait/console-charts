project ({{ .Chart.Version }}): release {{ .Release.Name | quote }}.
Identity labels on every resource: {{ include "project.helpers.identityLabels" . | trim | replace "\n" ", " | replace "\"" "" }}

{{- if eq (include "project.helpers.chartEnabled" .) "true" }}
Created:
  Project: {{ include "project.helpers.name" . }}
           (clusters: {{ range $i, $c := .Values.clusters }}{{ if $i }}, {{ end }}{{ $c.name }}{{ end }})
{{- else }}
Resources not created: enabled=false.
{{- end }}
